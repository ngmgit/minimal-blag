---
title: Projects
slug: "/projects"
---

import projects, { SectionStyled } from "./projects";

<div>
  {projects.map(
    (
      { startDate, endData, media, title, content, source: { label, value } },
      idx
    ) => (
      <SectionStyled key={idx}>
        <div className="title">
          <h3>{title}</h3>
          <div className="date-link-section">
            <span className="date">
              {startDate} : {endData || "Present"}
            </span>
            <span>
              <a href={value}>{label}</a>
            </span>
          </div>
        </div>
        <div>{media()}</div>
        <div>
          <ul>
            {content.map((text, idx) => (
              <li key={idx}>{text}</li>
            ))}
          </ul>
        </div>
      </SectionStyled>
    )
  )}
</div>
